import React from 'react';

export default function Pet(props) {
    return (
        <div className='pets'>
            {/* <h1 className='pets--name'>{props.pet.name}</h1> */}
        </div>
    )
}